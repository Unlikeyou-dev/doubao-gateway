# Skill: 验证

来源改造：Claude Code verify skill（改造为本地可执行版）

## 目标
证明改动在真实运行路径上有效，不只是“看起来对”。

## 流程
1. 明确改动面：哪些文件/行为变了
2. 找到用户可触达的表面：CLI 命令、HTTP 接口、脚本入口
3. 实际执行该路径，捕获输出
4. 对比期望
5. 失败则继续修，而不是只报告失败

## 证据
- 命令 + exit code + 关键输出
- 或接口响应关键字段
- 不要用“理论上应该可以”代替

## 输出
- 验证了什么
- 怎么跑的
- 结果
- 是否通过
